rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /__connection_checks/status {
      allow get: if true;
    }

    match /{document=**} {
      allow read, write: if false;
    }
  }
}
